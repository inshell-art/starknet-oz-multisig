{
  "policy_version": "1",
  "network_allowlist": ["sepolia", "mainnet", "devnet"],
  "lane_defaults": {
    "apply_allowed": false,
    "require_approval": false,
    "required_checks": ["bundle_manifest"],
    "allowed_ops": []
  },
  "lanes": {
    "lane0": {
      "apply_allowed": false,
      "require_approval": false,
      "required_checks": [],
      "allowed_ops": ["read"]
    },
    "lane1": {
      "apply_allowed": false,
      "require_approval": false,
      "required_checks": ["bundle_manifest"],
      "allowed_ops": ["plan"]
    },
    "lane2": {
      "apply_allowed": true,
      "require_approval": true,
      "required_checks": [
        "bundle_manifest",
        "chain_id",
        "signer",
        "target_identity",
        "preconditions",
        "simulation"
      ],
      "allowed_ops": ["declare", "deploy"]
    },
    "lane3": {
      "apply_allowed": true,
      "require_approval": true,
      "required_checks": [
        "bundle_manifest",
        "chain_id",
        "signer",
        "target_identity",
        "preconditions",
        "postconditions"
      ],
      "allowed_ops": ["invoke"]
    },
    "lane4": {
      "apply_allowed": true,
      "require_approval": true,
      "required_checks": [
        "bundle_manifest",
        "chain_id",
        "signer",
        "role_membership",
        "bounds",
        "postconditions"
      ],
      "allowed_ops": ["invoke"]
    },
    "lane5": {
      "apply_allowed": true,
      "require_approval": true,
      "required_checks": [
        "bundle_manifest",
        "chain_id",
        "signer",
        "target_identity",
        "preconditions",
        "postconditions",
        "multisig_threshold"
      ],
      "allowed_ops": ["invoke", "upgrade"]
    },
    "lane6": {
      "apply_allowed": true,
      "require_approval": true,
      "required_checks": [
        "bundle_manifest",
        "chain_id",
        "signer",
        "target_identity",
        "preconditions",
        "postconditions"
      ],
      "allowed_ops": ["invoke", "pause", "freeze"]
    }
  }
}
